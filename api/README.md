# Contrato da API

`openapi.yaml` — entregue na fase **F11** — documenta o contrato atual e as
adições opcionais, marcando o que depende de chave de configuração.

O contrato normativo hoje é `docs/ESPECIFICACAO.md` §1, com corpos de resposta
literais. O OpenAPI o descreve; não o substitui.
